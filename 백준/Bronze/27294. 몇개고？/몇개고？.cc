// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    int t, a;
    cin >> t >> a;
    if(a==0 && t >= 12 && t <= 16) cout << 320;
    else cout << 280;
    return 0;
}